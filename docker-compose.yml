services:
  bot:
    build: .
    container_name: ServerStats_bot
    restart: unless-stopped
    env_file:
      - .env
    volumes:
      - /var/run/docker.sock:/var/run/docker.sock:ro
    network_mode: host